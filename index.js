
// Write your code here
const num1 =31;
const num2 =2;
const multiply = num1 * num2;

const random =Math.ceil(Math.random()*10);
const num3 =14;
const num4 =10;
const mod = num3 % num4;

const max = Math.max(4,8,16,20,8);
function multiplyAndCheck(num1 ,num2) {
    const result=multiplyAndCheck(num1 ,num2);
    return result;
};
function generateRandomInteger() {
    return random;
};
const generatedRandom = generateRandomInteger();
const isIntegerGreaterThanZero = Number.isInteger(generatedRandom) && generatedRandom > 0;

function calculateMod(num3, num4) {
    return num3 % num4;
  }
  function findMax(...numbers) {
    return Math.max(...numbers);
  }
